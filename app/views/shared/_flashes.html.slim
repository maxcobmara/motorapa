- flash.each do |message_type, message|
  .alert.alert-dismissible.fade.show[class="alert-#{message_type == "notice" ? "success" : "danger"}" role="alert"]
    = message
    button[type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"]